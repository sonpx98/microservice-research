{
  "mcpServers": {
    "pika": {
      "command": "node",
      "args": ["apps/mcp/dist/index.js"],
      "env": {
        "API_URL": "http://127.0.0.1:3000/api",
        "API_KEY": "${API_KEY}",
        "TTS_OUTPUT_DIR": "${TTS_OUTPUT_DIR:-~/Downloads}"
      }
    }
  }
}
